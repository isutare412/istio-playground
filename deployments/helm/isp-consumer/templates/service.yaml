apiVersion: v1
kind: Service
metadata:
  name: {{ include "isp-consumer.fullname" . }}
  labels:
    {{- include "isp-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isp-consumer.selectorLabels" . | nindent 4 }}
